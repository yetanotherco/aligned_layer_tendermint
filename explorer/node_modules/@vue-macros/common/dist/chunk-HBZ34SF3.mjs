import {
  REGEX_LANG_TS
} from "./chunk-AYVTDBMV.mjs";

// src/lang.ts
import path from "path";
function getLang(filename) {
  return path.extname(filename).replace(/^\./, "");
}
function isTs(lang) {
  return lang && REGEX_LANG_TS.test(lang);
}

export {
  getLang,
  isTs
};
