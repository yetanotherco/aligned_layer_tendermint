import { buildExportCreators } from '../../utils';
const CREATOR_NAME = 'createPiniaStores';
export const createPiniaStoreFactory = (context, obj) => {
  return buildExportCreators(context, obj, CREATOR_NAME, ['ProtobufRpcClient'], 'usePiniaStore');
};