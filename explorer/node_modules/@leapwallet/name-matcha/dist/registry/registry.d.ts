import { AllowedTopLevelDomains, NameService, Network } from './name-service';
export declare const services: {
    icns: string;
    ibcDomains: string;
    stargazeNames: string;
    archIds: string;
};
export declare const allowedTopLevelDomains: AllowedTopLevelDomains;
export declare class Registry {
    private network;
    private services;
    constructor(network: Network);
    registerService(service: NameService): void;
    private getService;
    listServices(): string[];
    setNetwork(network: Network): void;
    getNetwork(): Network;
    resolve(name: string, serviceID: string, allowedTopLevelDomains?: AllowedTopLevelDomains): Promise<string>;
    lookup(address: string, serviceID: string): Promise<string>;
    resolveAll(name: string, allowedTopLevelDomains?: AllowedTopLevelDomains): Promise<Record<string, string | null>>;
    lookupAll(address: string): Promise<Record<string, string | null>>;
}
