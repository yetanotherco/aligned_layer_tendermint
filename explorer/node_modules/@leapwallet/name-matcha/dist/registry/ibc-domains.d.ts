import { AllowedTopLevelDomains, NameService, Network } from './name-service';
export declare const serviceID = "ibcDomains";
export declare class IBCDomains extends NameService {
    serviceID: string;
    chain: string;
    contractAddress: {
        mainnet: string;
        testnet: string;
    };
    resolve(name: string, network: Network, allowedTopLevelDomains?: AllowedTopLevelDomains): Promise<string>;
    lookup(address: string, network: Network): Promise<string>;
}
